import type { ReactNode } from "react";

/** Minimal chrome for print/PDF views (README_31). */
export default function PrintLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
