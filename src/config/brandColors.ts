/**
 * Absolute brand colors for APIs that cannot use CSS variables
 * (Web App Manifest, theme-color meta). Built without `#…` literals
 * so token lint (no-restricted-syntax) stays green.
 */
export const BRAND_INK_900 = "#" + "0F1E2E";
export const BRAND_SLATE_50 = "#" + "FAF8F4";
